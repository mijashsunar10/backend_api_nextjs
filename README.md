# backend_api_nextjs

# I have set up the postgresSQL and donload the pgadmin to use postrgeSql

# We will be using the PRISM ORM which will help us to create our table and connect with the postgrs Databbase

# We will aslo need an Rest Api client....We are gonna have multiple apis...We will be using requeslty which is light weight git freidnly...It is similar to postman but it is lightwight...For now i have added the requeslt in my browser as extension.....

# I have creted all the folder required for role based access and route.ts file

Now install required dependnecies
# npm install @prisma/client prisma bcryptjs jsonwebtoken
prisma (The Architect): It's a tool you use to design and build your database structure (tables, columns, etc.).
@prisma/client (The Bridge): It's a code library (generated by Prisma) that lets your Next.js app talk to your database (write queries, fetch data).
bcryptjs (The Locksmith): It's a tool for securely storing passwords. It scrambles them (hashes) so even if someone steals your database, they can't read the plain passwords.
jsonwebtoken (The Key Master): It's used to create and verify "tokens". Tokens are like digital keys that prove who a user is after they log in, allowing you to control what they can access.


# npm install -D @types/bcryptjs @types/jsonwebtoken
-D (Development Dependency): You are installing them only for your development environment. 
@types/...: Because bcryptjs and jsonwebtoken were originally written in plain JavaScript (not TypeScript), TypeScript doesn't know how to read them by default.
The Benefit: By installing these @types, you are giving TypeScript a "dictionary" of all the functions those libraries contai


# we need to setup add in the package.json    
"db:generate":"npx prisma generate",
    "db:push":"npx prisma db push",
    "db:seed":"npx tsx prisma/seed.ts",
    "db:migrate":"npx prisma migrate dev"
# THEN WE need to make server create nwo serve in pg admin and conenction also  and in env write connect database url from pg admin also and now we need to do .....
npm run db:generate

